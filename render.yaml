services:
  - type: web
    name: drug-discovery-ai-eval
    env: python
    plan: free
    runtime: python-3.12.8
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    healthCheckPath: /health
